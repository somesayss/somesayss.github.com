"use strict";
/**
 * 编译对象
 */
define(function(require, exports, module) {

	// 依赖
	var Class = require('class');

	// 类
	var CompileDom = Class.create({
		// 类名
		className: 'CompileDom',
		// 编译
		compile: function(){
			
		}
	});

	// 接口
	module.exports = CompileDom;
	
});