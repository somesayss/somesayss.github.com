"use strict";
/**
 * 进度类
 */
define(function(require, exports, module) {

	// 依赖
	var Widget = require('common/widget');

	// 类
	return Widget.extend({
		// 初始化私有属性
		setup: function(){
			// console.log('kiss', this.element);
			// console.log(this, this.get('aaaa'));
		}
	});


});